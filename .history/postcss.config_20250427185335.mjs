import tailwindcss from "@tailwindcss/postcss7-compat";

const config = {
  plugins: {
    tailwindcss: {}, // Gunakan plugin Tailwind CSS yang benar
    autoprefixer: {}, // Tambahkan autoprefixer jika diperlukan
  },
};

export default config;
